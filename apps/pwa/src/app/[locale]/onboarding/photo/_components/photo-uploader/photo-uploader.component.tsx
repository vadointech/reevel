"use client";

import { Button } from "@/components/ui";

export namespace OnboardingPhotoUploader {
    export type Props = {};
}

export const OnboardingPhotoUploader = ({}: OnboardingPhotoUploader.Props) => {
    return (
        <Button>
            Upload custom photo
        </Button>
    );
};